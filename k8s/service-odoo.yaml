apiVersion: v1
kind: Service
metadata:
  name: pos-dom
  namespace: pos-sale-dom
spec:
  selector:
    app: odoo
  ports:
    - port: 8069
      protocol: TCP
      targetPort: 8069
  type: ClusterIP
